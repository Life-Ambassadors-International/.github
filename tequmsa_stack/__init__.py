# TEQUMSA Three-Layer Compute Stack
# Layer 1: sovereign_inference_node  — FastAPI + HuggingFace Inference Endpoint
# Layer 2: qbec_ledger               — SQLite + HF Dataset on-space blockchain
# Layer 3: daemon_space               — self-fueling persistent processing loop
